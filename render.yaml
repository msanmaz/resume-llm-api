services:
  - type: web
    name: resume-llm-api
    runtime: node
    buildCommand: npm install
    startCommand: cd src && npm start
    envVars:
      - key: NODE_VERSION
        value: 18.0.0
    branch: main